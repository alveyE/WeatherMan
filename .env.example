# Wallet (never commit real key)
PRIVATE_KEY=0x...
FUNDER_ADDRESS=0x...   # Proxy if Polymarket account, else same as wallet

# Optional: Geocoding (Nominatim is free but rate-limited)
# GOOGLE_GEOCODING_API_KEY=...
